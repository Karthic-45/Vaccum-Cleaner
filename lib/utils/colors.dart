import 'package:flutter/material.dart';

// This is the main brand color for buttons, icons, and highlights.
// A nice, modern blue.
const Color primaryColor = Color(0xFF007BFF);

// This is the accent color, used for warnings or special highlights.
// The vibrant orange from your robot's dustbin.
const Color accentColor = Color(0xFFFFA500);

// The main background color for all screens.
// A soft, light grey.
const Color backgroundColor = Color(0xFFF0F2F5);

// The color for card backgrounds and app bars.
// Clean white for a modern look.
const Color cardColor = Colors.white;

// The color for primary text like titles and main content.
const Color textColor = Color(0xFF333333);

// A slightly lighter color for subtitles and less important text.
const Color secondaryTextColor = Color(0xFF6c757d);